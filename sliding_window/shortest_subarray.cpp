#include <iostream>
#include <vector>
using namespace std;
int main()
{
    vector<int> arr = {1, 5, 2, 6, 3, 8, 6, 4, 8, 4};

    // Similar to longest one but in this case just keep shrinking the window
    return 0;
}