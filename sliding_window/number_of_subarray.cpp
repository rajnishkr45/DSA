#include <iostream>
#include <vector>
using namespace std;
int main()
{
    vector<int> arr = {1, 5, 2, 6, 3, 8, 6, 4, 8, 4};

    // To find the number of subarray first of all find
    // x = Number of subarray whose sum is <= k
    // y = Number of subarray whose sum is <= k-1

    // Total number = x - y
    return 0;
}